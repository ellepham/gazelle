# Commands

Command wrappers are **generated by `install.sh`** into the target workspace (`.cursor/commands` or `.claude/commands`). Claude Code gets `/research`, `/discover`, `/design-critique`, etc. Cursor and Cowork use skills only (no command files).

This folder no longer contains per-command files (removed in v1.4 rename). The hub command is still `/gazelle`.
